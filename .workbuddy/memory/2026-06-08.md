# 2026-06-08 Memory

## Hugo Warmpaper 主题全面优化（16 项）

### 已完成的修改

**高优先级修复：**
1. `header.html` — 删除 `data-theme-state="light"` 硬编码，解决深色模式刷新后图标不同步
2. `list.html` — 分页 partial 调用参数修正为 `(dict "paginator" .Paginator)`，修复 section 列表页多页分页不显示
3. `style.css` — 合并 dark mode CSS 变量块，消除 34 行 DRY 违反；添加 `--color-table-stripe` 变量

**SEO & 可访问性增强：**
4. `head.html` — 添加 `<meta name="description">`（三级降级：front matter → .Summary 自动截取 → 站点描述）
5. `head.html` — 添加 `<link rel="canonical">`
6. `head.html` — 添加 RSS autodiscovery (`<link rel="alternate" type="application/rss+xml">`)
7. `head.html` — 字体版本从 `@latest` 固定为 `@1.1.0`
8. `profile.html` — avatar alt 文字语义化 + `fetchpriority="high"`
9. `single.html` — post-nav 链接添加 `aria-label`（上一篇/下一篇）

**代码质量改进：**
10. `footer.html` — 硬编码作者名 "awyme" 改为 `params.author | default .Site.Title`
11. `style.css` — table stripe 颜色从硬编码改为 CSS 变量 `--color-table-stripe`

**体验增强功能：**
12. `main.js` — 代码块一键复制按钮（自动注入 `.highlight` 容器，支持 fallback）
13. `main.js` — 阅读进度条（顶部固定进度指示器）
14. `style.css` — 复制按钮样式（hover 显示 + copied 状态）
15. `style.css` — 进度条样式（2.5px accent 色 + scaleX 动画）
16. `main.js` / `style.css` — 主题切换过渡动画（250ms 平滑切换，300ms 后移除 transition 防 FOUC）
17. `index.xml` — RSS 全文输出开关 `params.rss_full_content`

### 用户偏好
- 希望一次性完成所有优化而非逐项确认
- 关注 SEO 的 meta description 自动化生成能力（不希望每篇文章手动填写）
